import type React from 'react'
import { auth } from '@/lib/auth'
import { headers } from 'next/headers'
import { ArrowRight, BarChart3, Eye, Link2, ShieldCheck, Sparkles } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { ShortenerForm } from '@/components/shortener-form'

export default async function HomePage() {
  const session = await auth.api.getSession({ headers: await headers() })
  return <main className="min-h-dvh overflow-hidden bg-[radial-gradient(circle_at_75%_0%,_rgba(124,58,237,0.16),_transparent_35%)]"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 sm:px-8"><a href="/" className="flex items-center gap-2 text-sm font-bold tracking-[0.2em]"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground">S</span> SNIPLINK</a><nav className="flex items-center gap-3">{session ? <><a href="/dashboard" className="hidden text-sm font-semibold text-muted-foreground hover:text-foreground sm:block">Dashboard</a><a href="/dashboard" className="inline-flex h-7 items-center gap-1.5 rounded-lg bg-primary px-2.5 text-xs font-medium text-primary-foreground hover:bg-primary/80">Open workspace <ArrowRight className="size-3.5" /></a></> : <><a href="/sign-in" className="text-sm font-semibold text-muted-foreground hover:text-foreground">Sign in</a><a href="/sign-up" className="inline-flex h-7 items-center gap-1.5 rounded-lg bg-primary px-2.5 text-xs font-medium text-primary-foreground hover:bg-primary/80">Start earning <ArrowRight className="size-3.5" /></a></>}</nav></header><section className="mx-auto max-w-7xl px-5 pb-20 pt-16 sm:px-8 sm:pt-24"><div className="max-w-3xl"><div className="inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-3 py-1.5 text-xs font-semibold text-primary"><Sparkles className="size-3.5" /> The creator link platform</div><h1 className="mt-7 text-5xl font-bold leading-[1.05] tracking-[-0.05em] sm:text-7xl">Share more.<br /><span className="bg-gradient-to-r from-primary via-violet-500 to-cyan-500 bg-clip-text text-transparent">Earn from every view.</span></h1><p className="mt-7 max-w-2xl text-lg leading-8 text-muted-foreground sm:text-xl">Sniplink turns your audience into momentum. Create beautiful short links, understand your reach, and get rewarded when people click.</p></div><div className="mt-12 rounded-[2rem] border border-border bg-card p-4 shadow-2xl shadow-primary/10 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="font-semibold">Create your next link</p><p className="mt-1 text-sm text-muted-foreground">Anyone can shorten. Sign in to earn from qualified views.</p></div><Link2 className="size-5 text-primary" /></div><ShortenerForm />{!session ? <p className="mt-4 text-center text-xs text-muted-foreground"><a href="/sign-up" className="font-semibold text-primary hover:underline">Create an account</a> to save links, track views, and earn.</p> : null}</div><div className="mt-16 grid gap-4 sm:grid-cols-3"><Feature icon={<Eye />} title="Track every view" copy="See how your links perform with clear, useful analytics." /><Feature icon={<BarChart3 />} title="Know your value" copy="Understand estimated earnings with transparent CPM reporting." /><Feature icon={<ShieldCheck />} title="A better redirect" copy="A polished, private two-step experience for your audience." /></div></section><footer className="mx-auto max-w-7xl border-t border-border px-5 py-8 text-sm text-muted-foreground sm:px-8">Sniplink — link intelligence for the open web.</footer></main>
}
function Feature({ icon, title, copy }: { icon: React.ReactNode; title: string; copy: string }) { return <div className="rounded-2xl border border-border bg-card p-5"><div className="flex size-10 items-center justify-center rounded-xl bg-primary/10 text-primary">{icon}</div><h2 className="mt-5 font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{copy}</p></div> }
